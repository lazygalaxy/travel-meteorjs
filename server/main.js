import { Meteor } from 'meteor/meteor';

Meteor.startup(() => {
	//HTTP.call("GET", "https://en.wikipedia.org/w/api.php?action=query&titles=Pythagoreion&prop=revisions&rvprop=content&format=json", function (error, result) {
//	Assets.getText("Heraion_of_Samos.json", function (error, result) {
//		if (error) {
//			console.log(error);
//		} else {
//			var json = JSON.parse(result);
//			var pageKey = Object.keys(json.query.pages);
//			var page = json.query.pages[pageKey];
//
//			var doc = {};
//			processPage(doc, page);
//			console.info(doc);
//		}
//	});
});
